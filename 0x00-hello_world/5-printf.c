#include <stdio.h>
/**
 * main - Entry point
 * Description: return given string using printf
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammer, but the outcome is a piece os art,\n");
return (0);
}
